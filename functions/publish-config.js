/**
 * Private publish settings (this repo is private).
 * Change the password anytime by editing this file (or ask Cursor to change it).
 * Optional: put a GitHub token here so you do not need Cloudflare env vars.
 * Token needs Contents: Read and write on kenturnerlaw/kenturnerlaw.com.
 */
export const PUBLISH_PASSWORD = '4Y3asSIIVLgK7y';

// Optional. Prefer Cloudflare env GITHUB_TOKEN if set; otherwise use this.
export const GITHUB_TOKEN = '';
