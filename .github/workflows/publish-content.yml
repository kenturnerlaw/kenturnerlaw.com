name: Publish content pages

on:
  push:
    branches: [main]
    paths:
      - 'content/posts/**'
      - 'tools/publish/**'
      - 'package.json'
      - '.github/workflows/publish-content.yml'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Optional reason'
        required: false
        default: 'manual'

permissions:
  contents: write

jobs:
  build-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install publisher dependencies
        run: npm install --no-audit --no-fund

      - name: Check publisher syntax
        run: |
          node --check tools/publish/build-v2.js
          node --check tools/publish/enhance-seo.js
          node --check tools/publish/validate-amp.js
          node --check functions/api/publish.js

      - name: Build published AMP pages and automatic SEO
        run: npm run build:content

      - name: Validate generated AMP pages and SEO
        run: npm run validate:amp

      - name: Commit generated site files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            florida-criminal-defense-answers \
            florida-family-law-answers \
            florida-traffic-ticket-answers \
            florida-legal-answers \
            updates \
            sitemap.xml \
            sitemap.txt \
            search/published.json \
            content/posts \
            package-lock.json || true
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "Publish AMP articles with automatic SEO"
            git push
          fi
